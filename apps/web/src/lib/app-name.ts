/**
 * The product name, exactly as the chrome shows it — the brand in the header
 * and the root crumb of every trail. One constant so a rename is one edit;
 * "Retro #N" for a retrospective is the domain noun, not this.
 */
export const APP_NAME = 'Retroloop'
