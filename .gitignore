# git worktrees
.claude/worktrees/

node_modules/
dist/
# apps/web's second build: the same SPA with the typed tRPC mock linked in, which
# is what the web Gherkin suite drives (R-MOCK-LOCK).
dist-mocked/
.DS_Store

# build / tooling caches
*.tsbuildinfo
.vite/
.tanstack/

# playwright-bdd generated specs + playwright artifacts
.features-gen/
playwright-report/
test-results/
blob-report/

# machine-local Claude Code state
.claude/settings.local.json

# session reports are working artifacts, never merged
LANE_REPORT.md

# preview stages and screenshot scratch
preview-stage/
screenshots/

# machine-local repo instructions
CLAUDE.local.md
